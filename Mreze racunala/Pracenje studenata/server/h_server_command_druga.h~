#ifndef __SERVER_COMD_DRUGA_H_
#define __SERVER_COMD_DRUGA_H_

#include "h_server_command.h"

using namespace std;

class druga : public server_command {
	public:
		druga(
			data *studentsAndClassroomData,
			pthread_mutex_t *p_data_thread,
			pthread_mutex_t *p-output_thread
		);
		bool execute(int socket, char *message);
};

#endif
