#ifndef __CLIENT_COMD_DRUGA_H_
#define __CLIENT_COMD_DRUGA_H_

#include "h_client_command.h"
#include "h_tokenizer.h"

using namespace std;

class client_command_druga : public client_command {
	public:
		client_command();
		bool execute(int socket, tokenizer tokens);
};

#endif
